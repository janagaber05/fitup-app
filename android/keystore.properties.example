# Copy to keystore.properties (same folder) and fill in real values. Never commit keystore.properties.
# Paths are relative to the app module: android/app/
storeFile=../../../keys/fitup-release.jks
storePassword=your-store-password
keyAlias=fitup
keyPassword=your-key-password
